# vllm 0.19.1: CUDA 12.8 compatible (cluster tops out at 12.9; 0.20+ requires CUDA 13).
# Gemma4 dummy-input bug worked around via VLLM_USE_V1=0 in run.sbatch.
# The cluster's kernel driver natively supports CUDA 12.2; run.sbatch loads
# nvidia-hpc-sdk/25.3's forward-compat libcuda (570.124.06) to bridge to cu128.
vllm==0.19.1
transformers>=5.5.0
accelerate
pillow
numpy
pyyaml
huggingface_hub
# Build tools needed offline: pyairports is sdist-only and pip needs these to build it.
# pathlib is a no-op on Python 3 (stdlib) but pyairports' setup.py declares it as a build dep.
setuptools
wheel
pathlib
tensorflow-cpu
matplotlib
